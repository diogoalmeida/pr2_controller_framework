PR2 Cartesian Controllers
===
Package that implements cartesian controllers for the PR2. It is intended for these controllers to obey to a common interface that allows the to be called in the PR2 realtime loop. 